# -*- coding: utf-8 -*-
"""сцена_v2.html — показ «через процессы» поверх действующей сцены.

Совещание 10.09 (Жанна, Бахчан, Арман; созвон по переработке): в передел
проваливаемся не в веер систем, а в 4–5 шагов основного процесса; под шагом
— проекты, которые его ведут; снизу полоса «поддерживающая деятельность»
(закупки, промбезопасность, транспорт…). Наведение на поддерживающий проект
подсвечивает шаги, которые он охватывает (созвон по переработке: компьютерное
зрение — все пять, предиктивка — приём, переработка, хранение).

Действующая сцена не трогается: v2 собирается из неё этим скриптом, чтобы
правки сцены (тексты, ролики) подтягивались перезапуском. Стиль — наш:
палитра, шрифты, карточки как в презентациях. Клик по проекту открывает ту
же карточку/ролик/систему, что и в веере. Веер остаётся в коде, но не
вызывается.
Запуск из корня: python инструменты/собрать_v2.py
"""
import io
s = io.open('сцена.html', encoding='utf-8').read()
def зам(a, b):
    global s; assert s.count(a) == 1, a[:70]; s = s.replace(a, b)

# ---------- данные процессов (по драфту 10.09 и созвонам; формулировки на согласовании) ----------
ПРОЦЕССЫ = r'''
  /* ================= процессы переделов (v2, 10.09) =================
     Шаги — из драфта КОНЦЕПТ_ПРОЦЕССЫ_2026-09-10 (добыча и геология — по
     отчёту об операционной модели, слайды 60–61 и 87; переработка — по
     созвону 10.09; маркетинг — вечернее обсуждение 11.09). `проекты` — имена из ПЕРЕДЕЛЫ; `ведётся` — чем ведётся
     шаг, если проекта показа нет; `охват` у поддержки — индексы шагов. */
  var ПРОЦЕССЫ = {
    разведка: {
      /* 11.09, вечернее обсуждение с геологами: три шага — «поиск и оценка,
         реализация ГРР, подтверждение перспектив»; Geo AI — помощник для
         оценки и рисков на 1-м и 3-м, не «закрывает полностью»; вендоров
         (Petrel, Techlog, PGS) не показывать; поддержки у геологии нет. */
      шаги: [
        { имя:"Поиск и оценка участков", суть:"Гипотеза, оценка перспектив, права недропользования", проекты:["KMG Geo AI"] },
        { имя:"Реализация программы ГРР", суть:"Сейсморазведка 2D/3D, исследования, оценка запасов", проекты:["Масштабная программа геологоразведочных работ"], ведётся:"в работе: ML и нейросети (NN) в обработке и интерпретации 2D/3D" },
        { имя:"Подтверждение перспектив", суть:"Уточнение геологической картины и запасов, месторождение — в добычу", проекты:["KMG Geo AI"] }
      ],
      поддержка: [],
      примечание:"Три шага — вечернее обсуждение 11.09 с геологами; поддержки у геологии нет (закупки и TUMAR не используют)"
    },
    добыча: {
      /* 11.09, «Сквозной процесс Upstream»: четыре раздела верхнего уровня.
         Системы — только свои (вендоров не показываем); внутри «Добычи» —
         шесть процессов 2-го уровня из «Процесс по добыче». Procu — модуль
         KMG AISAP (Адиль). Мария: 3 % EBITDA — справочно в подписи. */
      шаги: [
        { имя:"Разработка месторождений", суть:"ГГДМ и цифровой двойник, стратегия разработки, ГТМ, заводнение, техрежим, точки бурения", проекты:["Цифровой двойник","ABAI — цифровой рейтинг новых скважин","ABAI — подбор и анализ эффективности ГТМ","ABAI — управление заводнением","ABAI — технологический режим"] },
        { имя:"Бурение и ВСР", суть:"Строительство скважин, ТКРС, подбор погружного оборудования, транспорт бригад", проекты:["ABAI — подбор ГНО","УТО — управление транспортным обеспечением"] },
        { имя:"Добыча", суть:"Учёт добычи · диспетчеризация и управление изменениями · потенциал и ИМА · подбор ГНО · энергоэффективность · надёжность трубопроводов", проекты:["ABAI — технологический режим","УТО — управление транспортным обеспечением"], ведётся:"шесть процессов 2-го уровня" },
        { имя:"КС и наземная инфраструктура", суть:"Концептуальный инжиниринг, проектирование и СМР новых мощностей, модель наземной инфраструктуры", проекты:["KMG AISAP"], ведётся:"Procu — модуль KMG AISAP · остальное в проработке" }
      ],
      поддержка: [
        { имя:"Промышленная безопасность", проекты:["TUMAR","ЭНД — наряды-допуски на промысле"], охват:[0,1,2,3] },
        { имя:"Закупки и снабжение", проекты:["KMG AISAP"], охват:[0,1,2,3] }
      ],
      примечание:"Четыре раздела — «Сквозной процесс Upstream» (11.09); распределение систем по разделам — из обсуждения, сверить с ABAI"
    },
    переработка: {
      шаги: [
        { имя:"Планирование производства", суть:"План выпуска и загрузка установок", проекты:[], ведётся:"в проработке" },
        { имя:"Приём сырья", суть:"Количество и качество", проекты:[], ведётся:"в системе управления производством" },
        /* 11.09: предиктивная аналитика — это шаг переработки, не поддержка;
           «планирование автоматизировано» снято — никто не подтверждал. */
        { имя:"Переработка сырья", суть:"Управление технологическим процессом, надёжность оборудования", проекты:["Предиктивная аналитика АНПЗ","PME — энергомониторинг"] },
        { имя:"Хранение продукции", суть:"Запасы и качество", проекты:[], ведётся:"в системе управления производством" },
        { имя:"Отгрузка продукции", суть:"Передача продукции, учёт", проекты:[], ведётся:"в проработке" }
      ],
      поддержка: [
        { имя:"Промышленная безопасность", проекты:["Компьютерное зрение — безопасность на заводе","ЭНД — наряды-допуски на заводе"], охват:[0,1,2,3,4] },
        { имя:"Подготовка операторов", проекты:["OTS — тренажёры операторов"], охват:[2] },
        { имя:"Закупки и снабжение", проекты:["KMG AISAP"], охват:[0,1,2,3,4] }
      ],
      примечание:"Пять шагов — созвон по переработке 10.09; предиктив в шаге переработки, статус планирования снят (11.09)"
    },
    маркетинг: {
      /* 11.09, вечернее обсуждение: шесть шагов по справке ДМНиН отклонены —
         «что это даёт в масштабе страны». Три шага: реализация (СДД), баланс
         (AIAN), отслеживание поставок (модуль отслеживания, не «КТЖ»). AISAP
         в маркетинге не показываем. */
      шаги: [
        { имя:"Реализация нефтепродуктов", суть:"Внутренний рынок по плану поставок МЭ РК и экспорт; договоры", проекты:["СДД — договорное делопроизводство"] },
        { имя:"Анализ баланса нефтепродуктов", суть:"Выпуск, запасы, поставки, потребление по регионам; дефицит и профицит", проекты:["KMG AIAN"] },
        { имя:"Отслеживание поставок", суть:"Дислокация вагонов, импорт, прибытие на нефтебазы", проекты:["Модуль отслеживания поставок нефтепродуктов"] }
      ],
      поддержка: [],
      примечание:"Три шага — вечернее обсуждение 11.09; формулировки сверить с ДМНиН"
    }
  };
'''
зам('  /* ================= веер систем =================', ПРОЦЕССЫ + '\n  /* ================= веер систем =================')

# Компьютерное зрение на заводе — проект, которого в сцене нет (созвон по переработке: «заводской TUMAR»)
зам('        { имя:"ЭНД — наряды-допуски на заводе",',
    '        { имя:"Компьютерное зрение — безопасность на заводе", состояние:"слайды", хроно:"—", эффект:"Контроль СИЗ и опасных зон на всех этапах завода · KPI уточняются", площадки:"АНПЗ · ПНХЗ · ПКОП", ярлыки:["ИИ"],\n          описание:"Компьютерное зрение на заводе — «заводской TUMAR»: охватывает все этапы от приёма сырья до отгрузки; масштаб и сложность больше промыслового. Запуск — до конца года." },\n        { имя:"ЭНД — наряды-допуски на заводе",')
зам('    "ЭНД — наряды-допуски на заводе":        "энд",', '    "ЭНД — наряды-допуски на заводе":        "энд",\n    "Компьютерное зрение — безопасность на заводе": "tumar",')

# ---------- CSS ----------
CSS = r'''
/* ================= v2: процессная схема передела ================= */
/* Лежит там же, где веер: фиксированный слой поверх сцены, в экранных
   координатах. Слева остаётся подпись передела и площадка под ней. */
#процесс{position:fixed;left:31%;right:2.5%;top:15%;bottom:4%;z-index:12;display:none;flex-direction:column;gap:20px;pointer-events:none;overflow:hidden}
body.вглубь #процесс{display:flex}
#процесс > *{pointer-events:auto}
#процесс .бровь{font-size:12px;letter-spacing:.22em;text-transform:uppercase;color:var(--приглуш);opacity:0;transition:opacity .4s ease}
#процесс.виден .бровь{opacity:1}
/* ряд шагов не растягивается на высоту панели: карточки высотой по содержимому,
   иначе три чипа в одной карточке вылезали за её низ (скрин Адиля 10.09) */
.шаги_процесса{display:flex;align-items:stretch;gap:0;flex:0 0 auto}
.шаг_процесса{flex:1 1 0;min-width:0;border:1px solid var(--грань);border-radius:20px;padding:18px 18px 16px;
  background:linear-gradient(180deg, rgba(12,22,38,.92), rgba(5,8,16,.72));display:flex;flex-direction:column;
  opacity:0;transform:translateY(14px);transition:opacity .45s ease, transform .45s cubic-bezier(.2,.7,.3,1), border-color .25s, box-shadow .25s}
#процесс.виден .шаг_процесса{opacity:1;transform:none}
.шаг_процесса.охват{border-color:rgba(62,230,184,.75);box-shadow:0 0 0 3px rgba(62,230,184,.14),0 0 28px rgba(62,230,184,.28)}
.шаг_процесса .номер{font-family:'Unbounded',system-ui,sans-serif;font-size:12px;letter-spacing:.2em;color:var(--кмг)}
.шаг_процесса h3{font-family:'Unbounded',system-ui,sans-serif;font-size:clamp(14px,calc(1.05 * 19.2px),20px);font-weight:700;line-height:1.2;margin-top:10px;color:var(--текст)}
.шаг_процесса .суть_шага{margin-top:8px;font-size:clamp(12px,calc(.8 * 19.2px),15px);line-height:1.45;color:var(--текст2)}
.шаг_процесса .ведётся{margin-top:auto;padding-top:14px;font-size:11.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--приглуш)}
.шаг_процесса .ведётся.проработка{color:var(--янтарь)}
.стрелка_шага{flex:0 0 26px;display:flex;align-items:center;justify-content:center;color:var(--циан);font-size:22px;opacity:.7}
/* проект под шагом: значок + имя, кликается как узел веера */
.проекты_шага{margin-top:14px;display:flex;flex-direction:column;gap:8px}
.проект_чип{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:12px;border:1px solid transparent;
  cursor:pointer;text-align:left;background:rgba(2,174,240,.06);transition:background .2s, border-color .2s}
.проект_чип:hover,.проект_чип:focus-visible{background:rgba(2,174,240,.14);border-color:rgba(2,174,240,.5);outline:none}
.проект_чип img{width:38px;height:38px;flex:0 0 38px;object-fit:contain;filter:drop-shadow(0 0 8px rgba(2,174,240,.5))}
.проект_чип .точка{width:14px;height:14px;flex:0 0 14px;margin:0 12px;border-radius:50%;background:var(--кмг)}
.проект_чип span{font-size:clamp(12px,calc(.82 * 19.2px),15px);font-weight:600;line-height:1.25;color:var(--текст)}
.проект_чип .печать_ии{position:static;width:24px;height:24px;flex:0 0 24px;font-size:8.5px;line-height:22px;transform:none;margin-left:auto}
/* полоса поддержки */
.поддержка{border-top:1px solid var(--грань);padding-top:14px;display:flex;flex-direction:column;gap:10px;flex:0 0 auto;
  opacity:0;transform:translateY(10px);transition:opacity .45s ease .25s, transform .45s ease .25s}
#процесс.виден .поддержка{opacity:1;transform:none}
.поддержка .ряд{display:flex;gap:14px;flex-wrap:wrap}
.группа_поддержки{flex:1 1 0;min-width:0;border:1px dashed rgba(124,141,166,.45);border-radius:16px;padding:12px 14px 12px}
.группа_поддержки .имя_группы{font-size:11.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--приглуш);margin-bottom:8px}
.группа_поддержки .проекты_шага{margin-top:0;flex-direction:row;flex-wrap:wrap}
.группа_поддержки .проект_чип{flex:1 1 200px}
#процесс .примечание{font-size:11.5px;color:var(--приглуш);letter-spacing:.02em}
/* в режиме v2 веер не рисуется */
body.процессы .веер{display:none}
'''
зам('.слайд,.низ{z-index:1}\n</style>', CSS + '</style>') if '.слайд,.низ{z-index:1}\n</style>' in s else зам('</style>\n</head>', CSS + '</style>\n</head>')

# ---------- разметка ----------
зам('<div class="веер" id="веер"></div>', '<div class="веер" id="веер"></div>\n<div id="процесс" aria-live="polite"></div>')

# ---------- JS: показ процесса вместо веера ----------
JS = r'''
  /* ================= v2: процессная схема ================= */
  var процесс = document.getElementById("процесс");
  document.body.classList.add("процессы");
  function найтиПроект(имя){
    for(var i = 0; i < ПЕРЕДЕЛЫ.length; i++){
      var б = ПЕРЕДЕЛЫ[i];
      for(var j = 0; j < б.проекты.length; j++) if(б.проекты[j].имя === имя) return { б:б, пр:б.проекты[j] };
    }
    return null;
  }
  function чипПроекта(имя, родитель){
    var н = найтиПроект(имя);
    var ч = узел("button", "проект_чип", родитель); ч.type = "button";
    if(!н){ ч.innerHTML = '<span class="точка"></span><span>' + имя + '</span>'; ч.disabled = true; return ч; }
    var знак = ИКОНКИ[имя];
    ч.innerHTML = (знак ? '<img src="медиа/иконки/' + знак + '.png" alt="">' : '<span class="точка"></span>') +
      '<span>' + имя + '</span>' +
      ((н.пр.ярлыки || []).indexOf("ИИ") >= 0 ? '<span class="печать_ии" aria-label="Искусственный интеллект">AI</span>' : '');
    ч.addEventListener("click", function(с){ с.stopPropagation(); открытьСистему(н.б, { проект:н.пр, группа:ч }); });
    return ч;
  }
  function показатьПроцесс(б){
    скрытьПроцесс();
    var д = ПРОЦЕССЫ[б.код]; if(!д) return;
    var бровь = узел("div", "бровь", процесс); бровь.textContent = "Основной производственный процесс · " + б.имя;
    var ряд = узел("div", "шаги_процесса", процесс);
    var карточки = д.шаги.map(function(ш, i){
      if(i){ var а = узел("div", "стрелка_шага", ряд); а.textContent = "→"; }
      var к = узел("div", "шаг_процесса", ряд);
      к.style.transitionDelay = (0.08 * i) + "s";
      к.innerHTML = '<div class="номер">0' + (i + 1) + '</div><h3>' + ш.имя + '</h3><div class="суть_шага">' + ш.суть + '</div>';
      if(ш.проекты && ш.проекты.length){
        var п = узел("div", "проекты_шага", к);
        ш.проекты.forEach(function(имя){ чипПроекта(имя, п); });
      }
      if(ш.ведётся){
        var в = узел("div", "ведётся" + (/проработк/.test(ш.ведётся) ? " проработка" : ""), к); в.textContent = ш.ведётся;
      }
      return к;
    });
    if(д.поддержка.length){   /* у геологии и маркетинга поддержки нет (11.09) */
    var под = узел("div", "поддержка", процесс);
    var б2 = узел("div", "бровь", под); б2.textContent = "Поддерживающая деятельность"; б2.style.opacity = "1";
    var р = узел("div", "ряд", под);
    д.поддержка.forEach(function(г){
      var гр = узел("div", "группа_поддержки", р);
      var и = узел("div", "имя_группы", гр); и.textContent = г.имя;
      var п = узел("div", "проекты_шага", гр);
      г.проекты.forEach(function(имя){ чипПроекта(имя, п); });
      /* охват: наведение подсвечивает шаги, которые ведёт эта поддержка
         (созвон по переработке: «наводим на компьютерное зрение — все пять
         блоков подсвечиваются») */
      function подсветить(да){ (г.охват || []).forEach(function(i){ if(карточки[i]) карточки[i].classList.toggle("охват", да); }); }
      гр.addEventListener("mouseenter", function(){ подсветить(true); });
      гр.addEventListener("mouseleave", function(){ подсветить(false); });
      гр.addEventListener("focusin", function(){ подсветить(true); });
      гр.addEventListener("focusout", function(){ подсветить(false); });
    });
    }
    /* примечания к схеме остаются в данных, на экран не выводятся (Адиль 10.09) */
    setTimeout(function(){ процесс.classList.add("виден"); }, 200);
  }
  function скрытьПроцесс(){ процесс.classList.remove("виден"); процесс.innerHTML = ""; }
'''
зам('  function открыть(п){', JS + '\n  function открыть(п){')
зам('    document.getElementById("где").textContent = СТАДИИ[п.стадия].имя + " / " + п.имя;\n    показатьУзлы(п);',
    '    document.getElementById("где").textContent = СТАДИИ[п.стадия].имя + " / " + п.имя;\n    /* v2: вместо веера — процессная схема */\n    показатьПроцесс(п);')
зам('    скрытьУзлы();\n    подпись_передела.classList.remove("виден");', '    скрытьУзлы();\n    скрытьПроцесс();\n    подпись_передела.classList.remove("виден");')
# площадка при наезде должна уместиться левее схемы: граница веера сдвигается с 60% к 44%
зам('  var ВЕЕР_X    = 60;', '  var ВЕЕР_X    = 32;   /* v2: правее начинается процессная схема */')
зам('  var ЦЕЛЬ_Y = 52;', '  var ЦЕЛЬ_Y = 64;   /* v2: площадка ниже, под текстом подписи (Адиль 10.09, стрелки на скрине) */')
# наезд v2: площадка стоит под подписью слева, схема справа не задевается
зам('''  function открыть(п){
    текущий = п;''', '''  /* v2: площадка не ищет просвет до веера, а встаёт в левую треть кадра под
     подписью — правее начинается схема, и площадка не должна её задевать. */
  function наездПодПросвет(){
    var сцШ = сцена.offsetWidth, сцЛево = (СТОЛ_Ш - сцШ) / 2;
    var просвет = 0.22 * СТОЛ_Ш;
    var к = Math.min(НАЕЗД_МАКС, просвет / (ШИРИНА_МОДУЛЯ/100 * сцШ));
    var серединаПx = 0.15 * СТОЛ_Ш;
    return { к:к, цельX: (серединаПx - сцЛево) / сцШ * 100 };
  }
  function открыть(п){
    текущий = п;''')
# карточка системы гасит узлы веера, а в v2 их нет — обход пустого списка
зам('''    б._узлы.forEach(function(у){
      у.группа.classList.toggle("ушёл", у.проект !== м.проект);
    });''', '''    (б._узлы || []).forEach(function(у){
      у.группа.classList.toggle("ушёл", у.проект !== м.проект);
    });''')
# кадр системы вырастает из узла веера; в v2 — из чипа проекта (м.группа)
зам('''    var свой = б._узлы.filter(function(у){ return у.проект === м.проект; })[0];
    вырастить(свой ? свой.группа : null, кадр, function(){''', '''    var свой = (б._узлы || []).filter(function(у){ return у.проект === м.проект; })[0];
    вырастить(свой ? свой.группа : (м.группа || null), кадр, function(){''')
зам('<title>КМГ — цифровая цепочка</title>', '<title>КМГ — цифровая цепочка · v2 процессы</title>')
io.open('сцена_v2.html', 'w', encoding='utf-8').write(s); print('ok', len(s))
